services:
  test:
    image: thyrlian/android-sdk:latest
    working_dir: /project
    volumes:
      - .:/project
    environment:
      - GRADLE_USER_HOME=/tmp/.gradle-cache
    command: >
      bash -c "
        chmod +x ./gradlew ./run_tests.sh &&
        ./run_tests.sh
      "
